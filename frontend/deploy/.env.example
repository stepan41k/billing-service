# Скопируй в deploy/.env и заполни

# Frontend
VITE_API_URL=
VITE_USE_MOCKS=false
VITE_ENV=production
FRONTEND_PORT=80

# Backend (proxy через nginx, менять не нужно)
# Остальные переменные бэка — в ../backend/.env или ../.env
